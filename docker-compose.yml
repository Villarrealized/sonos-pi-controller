version: "3"
services:
  web:
    container_name: sonos-pi-controller
    restart: always
    privileged: true
    build: .
    image: bytegenesis/sonos-pi-controller
    env_file: .env
    volumes:
      - ./:/usr/src/app
      
